import { PhaseNotice } from "@/components/site/PhaseNotice";

export default function ContactPage() {
  return <PhaseNotice eyebrow="ACT IV / WHAT COMES NEXT" title="Contact" description="The production page will provide direct email, LinkedIn, both GitHub identities, and an optional privacy-conscious contact form." />;
}
