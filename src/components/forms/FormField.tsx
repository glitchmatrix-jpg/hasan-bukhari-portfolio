import type { InputHTMLAttributes, TextareaHTMLAttributes } from "react";
interface Common { id: string; label: string; hint?: string; error?: string; }
type Props = (Common & { multiline?: false; inputProps?: Omit<InputHTMLAttributes<HTMLInputElement>, "id"> }) | (Common & { multiline: true; textareaProps?: Omit<TextareaHTMLAttributes<HTMLTextAreaElement>, "id"> });
export function FormField(props: Props) { const describedBy = [props.hint ? `${props.id}-hint` : null, props.error ? `${props.id}-error` : null].filter(Boolean).join(" ") || undefined; return <div className="form-field"><label htmlFor={props.id}>{props.label}</label>{props.multiline ? <textarea id={props.id} aria-describedby={describedBy} aria-invalid={Boolean(props.error)} {...props.textareaProps} /> : <input id={props.id} aria-describedby={describedBy} aria-invalid={Boolean(props.error)} {...props.inputProps} />}{props.hint ? <p id={`${props.id}-hint`} className="form-field__hint">{props.hint}</p> : null}{props.error ? <p id={`${props.id}-error`} className="form-field__error">{props.error}</p> : null}</div>; }
